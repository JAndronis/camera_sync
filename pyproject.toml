[project]
name = "camera-sync"
version = "0.1.0"
description = "Code that starts recording of the microscope camera for the levitator at cosaxs, based on software triggers."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "opencv-python",
    "sardana",
    "pandablocks",
    "flask",
    "requests",
    "ipython"
]
